# STM32-CAN-Bus-Vehicle-Diagnostic-Node
Build an STM32-based automotive diagnostic node that communicates with other ECUs over CAN and responds to basic diagnostic requests. Implement CAN frame handling, message filtering, diagnostic request parsing, and fault-code storage using Embedded C.
